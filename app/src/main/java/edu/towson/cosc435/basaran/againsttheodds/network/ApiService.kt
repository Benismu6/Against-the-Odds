/**
 * ApiService.kt
 *
 * This file defines the ApiService, which handles the API calls to fetch NFL data. It uses Retrofit
 * to communicate with the NFL API and converts the JSON responses into Kotlin data models.
 *
 * Responsibilities:
 * - Defines the network operations for fetching NFL game data.
 * - Configures the Retrofit interface for HTTP requests.
 */


package edu.towson.cosc435.basaran.againsttheodds.network

class ApiService {
}