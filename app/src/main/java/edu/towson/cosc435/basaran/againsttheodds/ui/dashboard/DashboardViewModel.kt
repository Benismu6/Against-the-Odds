/**
 * DashboardViewModel.kt
 *
 * This file defines the DashboardViewModel, which handles the logic and data management
 * for the DashboardFragment. It interacts with the GameRepository to fetch data from the NFL API.
 *
 * Responsibilities:
 * - Loads data about NFL games from the repository.
 * - Exposes LiveData to the DashboardFragment for UI updates.
 * - Manages the lifecycle of data to prevent memory leaks or unnecessary re-fetching.
 *
 * Key Methods:
 */

package edu.towson.cosc435.basaran.againsttheodds.ui.dashboard

class DashboardViewModel {
}